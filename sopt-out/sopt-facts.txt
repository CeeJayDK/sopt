# Input ranges for sopt-fx --facts: <file> <function> <input> = [lo, hi]
# Given ranges are used as facts. Commented lines are inputs without a known range,
# with a suggestion: check it, uncomment and rerun with --facts sopt-facts.txt.


# No known range:
# AdaptiveSharpen.fx AdaptiveSharpenP1 d[0] = [-1000, 1000]   # no guess (the default); 7 regions
# AdaptiveSharpen.fx AdaptiveSharpenP1 lowthr = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 lowthrsum = [-1000, 1000]   # no guess (the default); 2 regions
# AdaptiveSharpen.fx AdaptiveSharpenP1 luma[0] = [0, 1]   # name looks like a color; 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 luma[1] = [0, 1]   # name looks like a color; 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 luma[23] = [0, 1]   # name looks like a color; 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 luma[24] = [0, 1]   # name looks like a color; 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 neg_laplace = [-1000, 1000]   # no guess (the default); 2 regions
# AdaptiveSharpen.fx AdaptiveSharpenP1 neg_scale = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 nmax = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 nmin = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 pos_scale = [0, 7680]   # name looks like a pixel position; 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 sharpdiff = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 sharpdiff_lim = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 sharpen_val = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[0] = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[10] = [-1000, 1000]   # no guess (the default); 2 regions
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[11] = [-1000, 1000]   # no guess (the default); 2 regions
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[2] = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[5] = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[7] = [-1000, 1000]   # no guess (the default); 1 region
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[8] = [-1000, 1000]   # no guess (the default); 2 regions
# AdaptiveSharpen.fx AdaptiveSharpenP1 weights[9] = [-1000, 1000]   # no guess (the default); 2 regions
# AdaptiveSharpen.fx AdaptiveSharpenP1 weightsum = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_HGB hgb = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_Magic baseSample = [-1000, 1000]   # no guess (the default); 2 regions
# AmbientLight.fx PS_AL_Magic high = [-1000, 1000]   # no guess (the default); 6 regions
# AmbientLight.fx PS_AL_Magic highDirt = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_Magic highFlipOrig = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_Magic highLensSrc = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_Magic highOrig = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_Magic maxOrig = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_Magic maxhigh = [-1000, 1000]   # no guess (the default); 3 regions
# AmbientLight.fx PS_AL_Magic origBright = [-1000, 1000]   # no guess (the default); 1 region
# AmbientLight.fx PS_AL_VGB vgb = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx BloomPass0 tempbloom = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx GetAnamorphicSample coords = [0, 1]   # name looks like a texture coordinate; 3 regions
# Bloom.fx GetDistortedTex sample_center = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx GetDistortedTex sample_vector = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx GetDnB tex2Dlod(tex, float4(coords.xy, 0, 4)) = [0, 1]   # texture read (float format); 1 region
# Bloom.fx LensFlarePass0 anamFlare = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx LensFlarePass0 illuminationDecay = [0, 1]   # name looks like a color; 1 region
# Bloom.fx LensFlarePass0 lens = [-1000, 1000]   # no guess (the default); 2 regions
# Bloom.fx LensFlarePass0 lfcoord = [0, 1]   # name looks like a texture coordinate; 2 regions
# Bloom.fx LensFlarePass0 sample2 = [-1000, 1000]   # no guess (the default); 2 regions
# Bloom.fx LensFlarePass0 tempfact = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx LightingCombine anamflare = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx LightingCombine color = [0, 1]   # name looks like a color; 5 regions
# Bloom.fx LightingCombine colorbloom = [0, 1]   # name looks like a color; 6 regions
# Bloom.fx LightingCombine lensdirt = [-1000, 1000]   # no guess (the default); 3 regions
# Bloom.fx LightingCombine lensdirtmult = [-1000, 1000]   # no guess (the default); 1 region
# Bloom.fx LightingCombine tex2D(SamplerBloom3, texcoord) = [0, 1]   # texture read (float format); 3 regions
# Bloom.fx LightingCombine tex2D(SamplerBloom5, texcoord) = [0, 1]   # texture read (float format); 7 regions
# Bloom.fx LightingCombine tex2D(SamplerBloom5, texcoord.xy) = [0, 1]   # texture read (float format); 2 regions
# Border.fx BorderPass border = [-1000, 1000]   # no guess (the default); 1 region
# CAS.fx CASPass ampRGB = [0, 1]   # name looks like a color; 2 regions
# CAS.fx CASPass wRGB = [0, 1]   # name looks like a color; 1 region
# CRT.fx AdvancedCRTPass coeffs = [-1000, 1000]   # no guess (the default); 3 regions
# CRT.fx AdvancedCRTPass col = [0, 1]   # name looks like a color; 1 region
# CRT.fx AdvancedCRTPass col2 = [0, 1]   # name looks like a color; 1 region
# CRT.fx AdvancedCRTPass dotMaskWeights = [-1000, 1000]   # no guess (the default); 2 regions
# CRT.fx AdvancedCRTPass filter = [-1000, 1000]   # no guess (the default); 2 regions
# CRT.fx AdvancedCRTPass mul_res = [-1000, 1000]   # no guess (the default); 3 regions
# CRT.fx AdvancedCRTPass orig_xy = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx AdvancedCRTPass ratio_scale = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx AdvancedCRTPass uv_ratio = [0, 1]   # name looks like a texture coordinate; 1 region
# CRT.fx bkwtrans A = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx bkwtrans B = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx bkwtrans C = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx bkwtrans _point = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx bkwtrans c = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx bkwtrans poc = [-1000, 1000]   # no guess (the default); 3 regions
# CRT.fx bkwtrans r = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx bkwtrans tang = [-1000, 1000]   # no guess (the default); 3 regions
# CRT.fx bkwtrans uv = [0, 1]   # name looks like a texture coordinate; 1 region
# CRT.fx bkwtrans xy = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx corner coord = [0, 1]   # name looks like a texture coordinate; 6 regions
# CRT.fx corner dist = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx fwtrans r = [-1000, 1000]   # no guess (the default); 2 regions
# CRT.fx fwtrans uv = [0, 1]   # name looks like a texture coordinate; 1 region
# CRT.fx intersect A = [-1000, 1000]   # no guess (the default); 1 region
# CRT.fx intersect B = [-1000, 1000]   # no guess (the default); 2 regions
# CRT.fx intersect xy = [-1000, 1000]   # no guess (the default); 2 regions
# CRT.fx scanlineWeights color = [0, 1]   # name looks like a color; 3 regions
# CRT.fx scanlineWeights distance = [-1000, 1000]   # no guess (the default); 2 regions
# CRT.fx scanlineWeights weights = [-1000, 1000]   # no guess (the default); 2 regions
# CRT.fx scanlineWeights wid = [-1000, 1000]   # no guess (the default); 3 regions
# Colourfulness.fx Colourfulness c_diff = [-1000, 1000]   # no guess (the default); 1 region
# Curves.fx CurvesPass colorInput = [0, 1]   # name looks like a color; 1 region
# Curves.fx CurvesPass x = [-1000, 1000]   # no guess (the default); 10 regions
# DOF.fx BokehBlur blursamples = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx BokehBlur k = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx BokehBlur radiusCoeff = [-1000, 1000]   # no guess (the default); 2 regions
# DOF.fx BokehBlur res = [-1000, 1000]   # no guess (the default); 4 regions
# DOF.fx BokehBlur sampleOffset = [-1000, 1000]   # no guess (the default); 2 regions
# DOF.fx BokehBlur tap = [-1000, 1000]   # no guess (the default); 3 regions
# DOF.fx GetCoC scenecoc = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx GetCoC scenedepth = [0, 1]   # name looks like depth; 3 regions
# DOF.fx GetCoC scenefocus = [-1000, 1000]   # no guess (the default); 3 regions
# DOF.fx GetMatsoDOFBlur blurcolor = [0, 1]   # name looks like a color; 2 regions
# DOF.fx GetMatsoDOFBlur centerDepth = [0, 1]   # name looks like depth; 4 regions
# DOF.fx GetMatsoDOFBlur ct = [-1000, 1000]   # no guess (the default); 2 regions
# DOF.fx GetMatsoDOFBlur discRadius = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx GetMatsoDOFBlur taxis = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx GetMatsoDOFBlur w = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx GetMatsoDOFCA CoC = [-1000, 1000]   # no guess (the default); 2 regions
# DOF.fx GetMatsoDOFCA chroma = [-1000, 1000]   # no guess (the default); 3 regions
# DOF.fx GetMatsoDOFCA tex = [0, 1]   # name looks like a texture coordinate; 3 regions
# DOF.fx GetMatsoDOFCA tex2Dlod(col, float4(tb,0,0)) = [0, 1]   # texture read (float format); 1 region
# DOF.fx GetMatsoDOFCA tex2Dlod(col, float4(tg,0,0)) = [0, 1]   # texture read (float format); 1 region
# DOF.fx GetMatsoDOFCA tex2Dlod(col, float4(tr,0,0)) = [0, 1]   # texture read (float format); 1 region
# DOF.fx PS_GPDOF2 BiasCurve = [-1000, 1000]   # no guess (the default); 2 regions
# DOF.fx PS_GPDOF2 blurAmount = [-1000, 1000]   # no guess (the default); 2 regions
# DOF.fx PS_GPDOF2 blurcolor = [0, 1]   # name looks like a color; 6 regions
# DOF.fx PS_GPDOF2 centerDepth = [0, 1]   # name looks like depth; 4 regions
# DOF.fx PS_GPDOF2 currentVertex = [0, 1]   # name looks like a texture coordinate; 1 region
# DOF.fx PS_GPDOF2 discRadius = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_GPDOF2 nextVertex = [0, 1]   # name looks like a texture coordinate; 1 region
# DOF.fx PS_GPDOF2 sampleAngle = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_GPDOF2 tap = [-1000, 1000]   # no guess (the default); 3 regions
# DOF.fx PS_MagicDOF1 blurcolor = [0, 1]   # name looks like a color; 2 regions
# DOF.fx PS_MagicDOF2 blurcolor = [0, 1]   # name looks like a color; 2 regions
# DOF.fx PS_MagicDOF2 centerDepth = [0, 1]   # name looks like depth; 4 regions
# DOF.fx PS_MagicDOF2 discRadius = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_MagicDOF2 tapoffset1 = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_MatsoDOF4 centerDepth = [0, 1]   # name looks like depth; 4 regions
# DOF.fx PS_MatsoDOF4 discRadius = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_McFlyDOF2 centerDepth = [0, 1]   # name looks like depth; 4 regions
# DOF.fx PS_McFlyDOF2 chromadivisor = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_McFlyDOF2 discRadius = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_McFlyDOF2 scenecolor = [0, 1]   # name looks like a color; 1 region
# DOF.fx PS_McFlyDOF2 temphue = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_McFlyDOF3 blurcolor = [0, 1]   # name looks like a color; 1 region
# DOF.fx PS_RingDOF2 blurAmount = [-1000, 1000]   # no guess (the default); 3 regions
# DOF.fx PS_RingDOF2 blurcolor = [0, 1]   # name looks like a color; 2 regions
# DOF.fx PS_RingDOF2 centerDepth = [0, 1]   # name looks like depth; 4 regions
# DOF.fx PS_RingDOF2 discRadius = [-1000, 1000]   # no guess (the default); 1 region
# DOF.fx PS_RingDOF2 tap = [-1000, 1000]   # no guess (the default); 2 regions
# DPX.fx DPXPass c0 = [-1000, 1000]   # no guess (the default); 2 regions
# DPX.fx DPXPass color = [0, 1]   # name looks like a color; 1 region
# DPX.fx DPXPass luma = [0, 1]   # name looks like a color; 1 region
# Deband.fx PS_Deband k = [-1000, 1000]   # no guess (the default); 1 region
# Deband.fx PS_Deband sd = [-1000, 1000]   # no guess (the default); 1 region
# Deband.fx rand x = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_KNN counter = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_KNN iWindowArea = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_KNN result = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_KNN weight = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_NLM counter = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_NLM iWindowArea = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_NLM invBlockArea = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_NLM result = [-1000, 1000]   # no guess (the default); 1 region
# Denoise.fx PS_Denoise_NLM weight = [-1000, 1000]   # no guess (the default); 2 regions
# DisplayDepth.fx PS_DisplayDepth color = [0, 1]   # name looks like a color; 2 regions
# FXAA.fxh FxaaPixelShader lumaEndN = [0, 1]   # name looks like a color; 1 region
# FXAA.fxh FxaaPixelShader lumaEndP = [0, 1]   # name looks like a color; 1 region
# FXAA.fxh FxaaPixelShader lumaNN = [0, 1]   # name looks like a color; 2 regions
# FilmGrain.fx FilmGrainPass gauss_noise1 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain.fx FilmGrainPass r = [-1000, 1000]   # no guess (the default); 2 regions
# FilmGrain.fx FilmGrainPass stn = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain.fx FilmGrainPass variance = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx main noise = [-1000, 1000]   # no guess (the default); 2 regions
# FilmGrain2.fx pnoise3D grad001 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D grad010 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D grad011 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D grad100 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D grad101 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D grad110 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D grad111 = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D n_x = [-1000, 1000]   # no guess (the default); 2 regions
# FilmGrain2.fx pnoise3D n_xy = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx pnoise3D p = [-1000, 1000]   # no guess (the default); 1 region
# FilmGrain2.fx rnm tc = [-1000, 1000]   # no guess (the default); 1 region
# FilmicPass.fx FilmPass Cn = [-1000, 1000]   # no guess (the default); 1 region
# FilmicPass.fx FilmPass D = [-1000, 1000]   # no guess (the default); 17 regions
# FilmicPass.fx FilmPass F = [-1000, 1000]   # no guess (the default); 3 regions
# FilmicPass.fx FilmPass N = [-1000, 1000]   # no guess (the default); 2 regions
# FilmicPass.fx FilmPass iF = [-1000, 1000]   # no guess (the default); 6 regions
# FineSharp.fx PS_FineSharp_P3 o = [-1000, 1000]   # no guess (the default); 1 region
# FineSharp.fx PS_FineSharp_P3 sd = [-1000, 1000]   # no guess (the default); 1 region
# FineSharp.fx PS_FineSharp_P4 o = [-1000, 1000]   # no guess (the default); 3 regions
# FineSharp.fx PS_FineSharp_P4 t2 = [-1000, 1000]   # no guess (the default); 1 region
# FineSharp.fx PS_FineSharp_P4 t8 = [-1000, 1000]   # no guess (the default); 1 region
# FineSharp.fx PS_FineSharp_P5 edge = [-1000, 1000]   # no guess (the default); 1 region
# FineSharp.fx PS_FineSharp_P5 o = [-1000, 1000]   # no guess (the default); 1 region
# FineSharp.fx SharpDiff c = [-1000, 1000]   # no guess (the default); 1 region
# Glitch.fx fmod a = [-1000, 1000]   # no guess (the default); 4 regions
# Glitch.fx hsv2rgb c = [-1000, 1000]   # no guess (the default); 3 regions
# Glitch.fx noise p = [-1000, 1000]   # no guess (the default); 1 region
# Glitch.fx posterize color = [0, 1]   # name looks like a color; 1 region
# Glitch.fx posterize steps = [-1000, 1000]   # no guess (the default); 1 region
# Glitch.fx rand n = [-1000, 1000]   # no guess (the default); 2 regions
# Glitch.fx rgb2hsv c = [-1000, 1000]   # no guess (the default); 4 regions
# Glitch.fx rgb2hsv p = [-1000, 1000]   # no guess (the default); 1 region
# Glitch.fx rgb2hsv q = [-1000, 1000]   # no guess (the default); 2 regions
# HQ4X.fx PS_HQ4X w1 = [-1000, 1000]   # no guess (the default); 1 region
# HQ4X.fx PS_HQ4X w2 = [-1000, 1000]   # no guess (the default); 1 region
# HQ4X.fx PS_HQ4X w3 = [-1000, 1000]   # no guess (the default); 1 region
# HQ4X.fx PS_HQ4X w4 = [-1000, 1000]   # no guess (the default); 1 region
# HSLShift.fx HSLShift H0 = [-1000, 1000]   # no guess (the default); 2 regions
# HSLShift.fx HSLShift H1 = [-1000, 1000]   # no guess (the default); 2 regions
# HSLShift.fx HSLShift hsl = [-1000, 1000]   # no guess (the default); 1 region
# HSLShift.fx HSLShift w = [-1000, 1000]   # no guess (the default); 1 region
# HSLShift.fx RGB_to_HSL Delta = [-1000, 1000]   # no guess (the default); 2 regions
# HighPassSharpen.fx SharpBlurFinal color = [0, 1]   # name looks like a color; 2 regions
# HighPassSharpen.fx SharpBlurFinal luma = [0, 1]   # name looks like a color; 8 regions
# HighPassSharpen.fx SharpBlurFinal sharp = [-1000, 1000]   # no guess (the default); 11 regions
# HighPassSharpen.fx SharpBlurFinal sharpMax = [-1000, 1000]   # no guess (the default); 1 region
# HighPassSharpen.fx SharpBlurFinal sharpMin = [-1000, 1000]   # no guess (the default); 1 region
# Levels.fx LevelsPass white_point_float = [-1000, 1000]   # no guess (the default); 1 region
# LevelsPlus.fx RRTAndODTFit v = [-1000, 1000]   # no guess (the default); 3 regions
# LightDoF.fx getFocus depth = [0, 1]   # name looks like depth; 2 regions
# LightDoF.fx poisson basis = [-1000, 1000]   # no guess (the default); 1 region
# LightDoF.fx poisson col = [0, 1]   # name looks like a color; 1 region
# LightDoF.fx poisson tex2Dlod(sp, float4(bCoord, 0, 0)) = [0, 1]   # texture read (float format); 1 region
# LightDoF.fx poisson tex2Dlod(sp, float4(gCoord, 0, 0)) = [0, 1]   # texture read (float format); 1 region
# LightDoF.fx poisson tex2Dlod(sp, float4(rCoord, 0, 0)) = [0, 1]   # texture read (float format); 1 region
# LumaSharpen.fx LumaSharpenPass sharp_strength_luma_clamp = [0, 1]   # name looks like a color; 1 region
# MagicBloom.fx PS_Blend tex2D(sMagicBloom_Adapt, 0.0) = [0, 1]   # texture read (float format); 1 region
# MagicBloom.fx PS_Blur1 col = [0, 1]   # name looks like a color; 2 regions
# MagicBloom.fx PS_GetAdapt curr = [-1000, 1000]   # no guess (the default); 2 regions
# MagicBloom.fx PS_GetAdapt tex2Dlod(sMagicBloom_Small, float4(0.5, 0.5, 0, lowest_mip - iAdapt_Precision)) = [0, 1]   # texture read (float format); 1 region
# MagicBloom.fx blend_screen a = [-1000, 1000]   # no guess (the default); 1 region
# MagicBloom.fx blend_screen b = [-1000, 1000]   # no guess (the default); 1 region
# MagicBloom.fx tonemap col = [0, 1]   # name looks like a color; 4 regions
# MagicBloom.fx tonemap exposure = [0, 7680]   # name looks like a pixel position; 2 regions
# ReShade.fxh GetLinearizedDepth depth = [0, 1]   # name looks like depth; 2 regions
# ReShade.fxh GetLinearizedDepth texcoord = [0, 1]   # name looks like a texture coordinate; 2 regions
# ReflectiveBumpMapping.fx GetBlendedNormals n1 = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetBlendedNormals n2 = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetHueMask H = [-1000, 1000]   # no guess (the default); 16 regions
# ReflectiveBumpMapping.fx GetHueMask SMod = [-1000, 1000]   # no guess (the default); 13 regions
# ReflectiveBumpMapping.fx GetNormalFromColor ddx = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetNormalFromColor ddy = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetNormalFromColor hmx = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetNormalFromColor hmy = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetNormalFromColor hpx = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetNormalFromColor hpy = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx GetNormalFromColor offset = [-1000, 1000]   # no guess (the default); 2 regions
# ReflectiveBumpMapping.fx GetNormalFromColor scale = [-1000, 1000]   # no guess (the default); 4 regions
# ReflectiveBumpMapping.fx GetNormalFromColor xymult = [-1000, 1000]   # no guess (the default); 3 regions
# ReflectiveBumpMapping.fx GetPosition EyeDepth = [0, 1]   # name looks like depth; 1 region
# ReflectiveBumpMapping.fx PS_RBM_Gen SceneNormals = [-1000, 1000]   # no guess (the default); 4 regions
# ReflectiveBumpMapping.fx PS_RBM_Gen SchlickReflectance = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx PS_RBM_Gen SurfaceNormals = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx PS_RBM_Gen bump = [-1000, 1000]   # no guess (the default); 1 region
# ReflectiveBumpMapping.fx PS_RBM_Gen colorMask = [0, 1]   # name looks like a color; 1 region
# ReflectiveBumpMapping.fx PS_RBM_Gen cosphi = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAACalculateDiagWeights c = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAACalculatePredicatedThreshold neighbours = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgeDetectionPS C = [-1000, 1000]   # no guess (the default); 4 regions
# SMAA.fxh SMAAColorEdgeDetectionPS Cbottom = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgeDetectionPS Cleft = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAAColorEdgeDetectionPS Cleftleft = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgeDetectionPS Cright = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgeDetectionPS Ctop = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAAColorEdgeDetectionPS Ctoptop = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgeDetectionPS delta = [-1000, 1000]   # no guess (the default); 3 regions
# SMAA.fxh SMAAColorEdgeDetectionPS finalDelta = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgeDetectionPS maxDelta = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAAColorEdgeDetectionPS t = [-1000, 1000]   # no guess (the default); 6 regions
# SMAA.fxh SMAAColorEdgePredicationDetectionPS C = [-1000, 1000]   # no guess (the default); 4 regions
# SMAA.fxh SMAAColorEdgePredicationDetectionPS Cbottom = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgePredicationDetectionPS Cleft = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAAColorEdgePredicationDetectionPS Cleftleft = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgePredicationDetectionPS Cright = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgePredicationDetectionPS Ctop = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAAColorEdgePredicationDetectionPS Ctoptop = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgePredicationDetectionPS delta = [-1000, 1000]   # no guess (the default); 3 regions
# SMAA.fxh SMAAColorEdgePredicationDetectionPS finalDelta = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAAColorEdgePredicationDetectionPS maxDelta = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAAColorEdgePredicationDetectionPS t = [-1000, 1000]   # no guess (the default); 6 regions
# SMAA.fxh SMAADecodeDiagBilinearAccess e = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAADepthEdgeDetectionPS neighbours = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAADetectHorizontalCornerPattern d = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAADetectHorizontalCornerPattern factor = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAADetectHorizontalCornerPattern weights = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAADetectVerticalCornerPattern d = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAADetectVerticalCornerPattern factor = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAADetectVerticalCornerPattern weights = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgeDetectionPS L = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAALumaEdgeDetectionPS Lbottom = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgeDetectionPS Lleft = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAALumaEdgeDetectionPS Lleftleft = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgeDetectionPS Lright = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgeDetectionPS Ltop = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAALumaEdgeDetectionPS Ltoptop = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgeDetectionPS delta = [-1000, 1000]   # no guess (the default); 3 regions
# SMAA.fxh SMAALumaEdgeDetectionPS finalDelta = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgeDetectionPS maxDelta = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAALumaEdgePredicationDetectionPS L = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAALumaEdgePredicationDetectionPS Lbottom = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgePredicationDetectionPS Lleft = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAALumaEdgePredicationDetectionPS Lleftleft = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgePredicationDetectionPS Lright = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgePredicationDetectionPS Ltop = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAALumaEdgePredicationDetectionPS Ltoptop = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgePredicationDetectionPS delta = [-1000, 1000]   # no guess (the default); 3 regions
# SMAA.fxh SMAALumaEdgePredicationDetectionPS finalDelta = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAALumaEdgePredicationDetectionPS maxDelta = [-1000, 1000]   # no guess (the default); 2 regions
# SMAA.fxh SMAANeighborhoodBlendingPS a = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAANeighborhoodBlendingPS blendingWeight = [-1000, 1000]   # no guess (the default); 1 region
# SMAA.fxh SMAASearchDiag1 coord = [0, 1]   # name looks like a texture coordinate; 1 region
# SMAA.fxh SMAASearchDiag2 coord = [0, 1]   # name looks like a texture coordinate; 1 region
# Technicolor.fx TechnicolorPass negative_mul_b = [-1000, 1000]   # no guess (the default); 1 region
# Technicolor.fx TechnicolorPass negative_mul_g = [-1000, 1000]   # no guess (the default); 1 region
# Technicolor.fx TechnicolorPass negative_mul_r = [-1000, 1000]   # no guess (the default); 1 region
# Tonemap.fx TonemapPass color = [0, 1]   # name looks like a color; 6 regions
# Tonemap.fx TonemapPass diffcolor = [0, 1]   # name looks like a color; 1 region
# Tonemap.fx TonemapPass lum = [0, 1]   # name looks like a color; 4 regions
# Vignette.fx VignettePass color = [0, 1]   # name looks like a color; 6 regions
# macro global PI = [0, 6.2831854]   # preprocessor definition, twice its value; 1 region
# macro global RESHADE_DEPTH_INPUT_X_OFFSET = [-1, 1]   # preprocessor definition (value 0); 1 region
# macro global RESHADE_DEPTH_INPUT_Y_OFFSET = [-1, 1]   # preprocessor definition (value 0); 1 region
# macro global RESHADE_DEPTH_LINEARIZATION_FAR_PLANE = [0, 2000]   # preprocessor definition, twice its value; 3 regions
# macro global RESHADE_DEPTH_MULTIPLIER = [0, 2]   # preprocessor definition, twice its value; 2 regions
# macro global SMAA_LOCAL_CONTRAST_ADAPTATION_FACTOR = [0, 4]   # preprocessor definition, twice its value; 8 regions
